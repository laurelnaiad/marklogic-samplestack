define(['testHelper'], function (helper) {

  return function () {

    var mlModelSpec;
    describe('mlModelSpec', function () {
      // var mlUtil;
      // var $q;
      // var $http;
      // var $httpBackend;
      // var $rootScope;
      // var mlModelBase;
      // var mlSchema;
      // var impl1;
      // var impl2;

      beforeEach(function (done) {
        module('_marklogic');
        inject(function (
          _mlModelSpec_
        ) {
          mlModelSpec = _mlModelSpec_;
        });

      });
    });
  };
});
